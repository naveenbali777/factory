import { Model } from "./base/model.base";

export class PipoInformations extends Model {
  countryOfShipment: string;
  piNumber: string;
  pipoKey: string;
  poNumber: string;
  productCategory: string;
  productLine: string;
}

